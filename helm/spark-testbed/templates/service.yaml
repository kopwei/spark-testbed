apiVersion: v1
kind: Service
metadata:
  name: {{ include "spark-testbed.fullname" . }}
  labels:
    {{- include "spark-testbed.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spark-testbed.selectorLabels" . | nindent 4 }}
